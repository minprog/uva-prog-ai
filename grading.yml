grades:
    simple:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    breakout:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    hangman:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    yahtzee:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    namesurf:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    q1:
        type: float
        subgrades:
            points: integer
        calculation: (points / 29.0 * 9 + 1).round(1)
    q2:
        type: float
        subgrades:
            points: integer
        calculation: (points / 24.0 * 9 + 1).round(1)
    resit:
        type: float
        subgrades:
            points: integer
        calculation: (points / 46.0 * 9 + 1).round(1)

calculation:
    resit:
        resit: 1
        psets: 1
    normal:
        quizzes: 1
        psets: 1

psets:
    submits:
        simple: 1
        breakout: 1
        hangman: 1
        yahtzee: 1
        namesurf: 2

quizzes:
    submits:
        q1: 1
        q2: 1

resit:
    submits:
        resit: 1
